{
  "name": "jev-vercel-gateway-poc",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Jev evaluation proof of concept using fictional market data",
  "type": "module",
  "scripts": {
    "validate": "node jev-test.js --validate",
    "test:jev": "node jev-test.js",
    "validate:benchmark": "node jev-benchmark.js --validate",
    "benchmark:jev": "node jev-benchmark.js",
    "validate:sensitivity": "node jev-sensitivity.js --validate",
    "sensitivity:jev": "node jev-sensitivity.js",
    "test:historical": "node --test historical-validation.test.js",
    "historical:generate": "node historical-validation.js generate",
    "historical:diagnostic": "node historical-validation.js diagnostic"
  },
  "engines": {
    "node": ">=20"
  }
}
